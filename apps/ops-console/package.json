{
  "name": "@helix/ops-console",
  "version": "0.0.0",
  "private": true,
  "description": "Operations console application shell.",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests --config ../../vitest.config.ts --dir .",
    "lint": "eslint . --max-warnings 0"
  },
  "type": "module"
}
