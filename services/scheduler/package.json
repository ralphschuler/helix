{
  "name": "@helix/scheduler",
  "version": "0.0.0",
  "private": true,
  "description": "Schedules, durable timers, and wake-up service shell.",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run --passWithNoTests --config ../../vitest.config.ts --dir .",
    "lint": "eslint . --max-warnings 0"
  },
  "type": "module",
  "devDependencies": {
    "eslint": "^10.3.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.6"
  }
}
